const layarInput = document.querySelector(".input");
const angka = document.querySelectorAll(".angka");
const reset = document.querySelectorAll(".reset");
const hasil = document.querySelectorAll(".hasil");

angka.forEach.addEventListener("click", function() {
    alert("ok");
});